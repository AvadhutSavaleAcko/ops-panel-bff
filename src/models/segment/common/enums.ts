export enum fuelType {
    "internal_lpg_cylinder", "petrol", "diesel", null
}

export enum planType {
    "comprehensive", "third_party", "bundled", "od_only", null
}

export enum planSelected {
    "car_tp", "car_od"
}